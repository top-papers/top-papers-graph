# Исправление offline review (Task 2)

Что исправлено:
- устранена JS syntax error в автономной форме expert validation;
- причина была в многострочном placeholder для YAML-исключений, встроенном в одинарную JS-строку;
- placeholder переведён на template literal, поэтому встроенный скрипт корректно парсится и выполняется;
- добавлен регрессионный тест, который проверяет валидность встроенного JavaScript у собранного offline review.

Эффект:
- снова работают кнопки верхнего меню;
- отображаются встроенные данные графов и assertions;
- доступны autosave / draft JSON / export ZIP.
