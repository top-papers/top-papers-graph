# Multimodal reviews

Цель: позволить экспертам быстро исправлять/подтверждать результаты VLM парсинга PDF страниц
(подписи, таблицы, формулы) и тем самым:

1) собрать высококачественные пары (image/page → caption/table/equations)
2) улучшить пост‑обработку (структурирование) мультимодальных артефактов

## Формат

Каждый файл — JSON с набором `items`.

См. `_template.json`.