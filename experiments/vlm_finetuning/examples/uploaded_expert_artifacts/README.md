# Uploaded expert artifacts copied into the fine-tuning bundle

This folder mirrors the user-provided sample YAML trajectories and Task 1/Task 2 notebooks,
so the bundle can be used immediately for schema inspection, sanity-checking dataset builders,
and preparing future annotation batches.
